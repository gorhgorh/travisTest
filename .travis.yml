language: node_js
node_js:
  - "7"

install:
  - npm install
  - npm test

notifications:
  irc:
    channels:
      - "chat.freenode.net#cibot"
    on_success: change # default: always
    on_failure: always # default: always
